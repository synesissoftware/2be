name: CI cell

on:
  workflow_call:
    inputs:
      cell-id:
        required: true
        type: string
      os:
        required: true
        type: string
      c-compiler:
        required: true
        type: string
      cpp-compiler:
        required: true
        type: string
      build-type:
        required: false
        type: string
        default: Release
      run-tests:
        required: false
        type: boolean
        default: true

jobs:
  cell:
    name: ${{ inputs.cell-id }}
    runs-on: ${{ inputs.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Clang (Ubuntu)
        if: runner.os == 'Linux' && inputs.c-compiler == 'clang'
        run: sudo apt-get update && sudo apt-get install -y clang

      - name: Setup MSYS2 MinGW
        if: runner.os == 'Windows' && inputs.c-compiler == 'mingw'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-toolchain
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-make

      - name: Install dependencies
        shell: bash
        run: |
          set -euxo pipefail

          DEPS="${RUNNER_TEMP}/sis-deps"
          mkdir -p "$DEPS"
          echo "SIS_DEPS=$DEPS" >> "$GITHUB_ENV"

          if [ "${{ inputs.c-compiler }}" = "mingw" ]; then
            export PATH="/mingw64/bin:$PATH"
          fi

          cmake_configure() {
            local src="$1" bld="$2"
            shift 2
            if [ "${{ inputs.c-compiler }}" = "cl" ]; then
              cmake -S "$src" -B "$bld" \
                -DCMAKE_DISABLE_FIND_PACKAGE_MFC=TRUE \
                -DCMAKE_INSTALL_PREFIX="$DEPS" \
                -DCMAKE_PREFIX_PATH="$DEPS" \
                -DBUILD_EXAMPLES=OFF \
                -DBUILD_TESTING=OFF \
                "$@"
            elif [ "${{ inputs.c-compiler }}" = "mingw" ]; then
              cmake -S "$src" -B "$bld" -G "MinGW Makefiles" \
                -DCMAKE_BUILD_TYPE="${{ inputs.build-type }}" \
                -DCMAKE_INSTALL_PREFIX="$DEPS" \
                -DCMAKE_PREFIX_PATH="$DEPS" \
                -DBUILD_EXAMPLES=OFF \
                -DBUILD_TESTING=OFF \
                "$@"
            else
              cmake -S "$src" -B "$bld" \
                -DCMAKE_BUILD_TYPE="${{ inputs.build-type }}" \
                -DCMAKE_C_COMPILER="${{ inputs.c-compiler }}" \
                -DCMAKE_CXX_COMPILER="${{ inputs.cpp-compiler }}" \
                -DCMAKE_INSTALL_PREFIX="$DEPS" \
                -DCMAKE_PREFIX_PATH="$DEPS" \
                -DBUILD_EXAMPLES=OFF \
                -DBUILD_TESTING=OFF \
                "$@"
            fi
          }

          cmake_build_install() {
            local bld="$1"
            if [ "${{ inputs.c-compiler }}" = "cl" ]; then
              cmake --build "$bld" --config "${{ inputs.build-type }}" --parallel
              cmake --install "$bld" --config "${{ inputs.build-type }}"
            elif [ "${{ inputs.c-compiler }}" = "mingw" ]; then
              cmake --build "$bld" --parallel 2
              cmake --install "$bld"
            else
              cmake --build "$bld" --parallel
              cmake --install "$bld"
            fi
          }

          # STLSoft (required when BUILD_TESTING=ON)
          STLSRC="${RUNNER_TEMP}/stlsoft-src"
          STLBUILD="${RUNNER_TEMP}/stlsoft-build"
          git clone --depth 1 https://github.com/synesissoftware/STLSoft "$STLSRC"
          cmake_configure "$STLSRC" "$STLBUILD"
          cmake_build_install "$STLBUILD"

          # xTests (test-only)
          XTSRC="${RUNNER_TEMP}/xtests-src"
          XTBUILD="${RUNNER_TEMP}/xtests-build"
          git clone --depth 1 https://github.com/synesissoftware/xTests "$XTSRC"
          cmake_configure "$XTSRC" "$XTBUILD" -DNO_SHWILD=TRUE
          cmake_build_install "$XTBUILD"

      - name: Configure
        shell: bash
        run: |
          set -euo pipefail
          DEPS="${{ env.SIS_DEPS }}"

          if [ "${{ inputs.c-compiler }}" = "mingw" ]; then
            export PATH="/mingw64/bin:$PATH"
          fi

          if [ "${{ inputs.c-compiler }}" = "cl" ]; then
            cmake -B build -S . \
              -DCMAKE_DISABLE_FIND_PACKAGE_MFC=TRUE \
              -DCMAKE_PREFIX_PATH="$DEPS" \
              -DBUILD_EXAMPLES=OFF \
              -DBUILD_TESTING=ON
          elif [ "${{ inputs.c-compiler }}" = "mingw" ]; then
            cmake -B build -G "MinGW Makefiles" \
              -DCMAKE_BUILD_TYPE="${{ inputs.build-type }}" \
              -DCMAKE_C_COMPILER=gcc \
              -DCMAKE_CXX_COMPILER=g++ \
              -DCMAKE_PREFIX_PATH="$DEPS" \
              -DBUILD_EXAMPLES=OFF \
              -DBUILD_TESTING=ON
          else
            cmake -B build -S . \
              -DCMAKE_BUILD_TYPE="${{ inputs.build-type }}" \
              -DCMAKE_C_COMPILER="${{ inputs.c-compiler }}" \
              -DCMAKE_CXX_COMPILER="${{ inputs.cpp-compiler }}" \
              -DCMAKE_PREFIX_PATH="$DEPS" \
              -DBUILD_EXAMPLES=OFF \
              -DBUILD_TESTING=ON
          fi

      - name: Build
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ inputs.c-compiler }}" = "mingw" ]; then
            export PATH="/mingw64/bin:$PATH"
          fi
          if [ "${{ inputs.c-compiler }}" = "cl" ]; then
            cmake --build build --config "${{ inputs.build-type }}" --parallel
          elif [ "${{ inputs.c-compiler }}" = "mingw" ]; then
            cmake --build build --parallel 2
          else
            cmake --build build --parallel
          fi

      - name: Run tests
        if: inputs.run-tests
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ inputs.c-compiler }}" = "mingw" ]; then
            export PATH="/mingw64/bin:$PATH"
          fi
          export SIS_CMAKE_BUILD_DIR="${{ github.workspace }}/build"
          if [ "${{ runner.os }}" = "Windows" ]; then
            if [ -f run_all_unit_tests.cmd ]; then
              cmd.exe //c "run_all_unit_tests.cmd -M -v"
            else
              ./run_all_unit_tests.sh -M -v
            fi
          else
            ./run_all_unit_tests.sh -M -v
          fi

      - name: Install and verify
        shell: bash
        run: |
          set -euo pipefail
          PREFIX="${RUNNER_TEMP}/install-prefix"
          if [ "${{ inputs.c-compiler }}" = "mingw" ]; then
            export PATH="/mingw64/bin:$PATH"
          fi
          if [ "${{ inputs.c-compiler }}" = "cl" ]; then
            cmake --install build --config "${{ inputs.build-type }}" --prefix "$PREFIX"
          else
            cmake --install build --prefix "$PREFIX"
          fi
          test -f "$PREFIX/include/2be/2be.h"
          if [ "${{ inputs.c-compiler }}" = "cl" ]; then
            test -f "$PREFIX/lib/2be.lib" || test -f "$PREFIX/lib/lib2be.lib"
          else
            test -f "$PREFIX/lib/lib2be.a"
          fi
          test -f "$PREFIX/lib/cmake/2be/2be-config.cmake"
